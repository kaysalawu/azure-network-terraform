version: '3'
services:
  app1:
    container_name: app1
    build:
      context: ./app1
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    network_mode: host

  app2:
    container_name: app2
    build:
      context: ./app2
      dockerfile: Dockerfile
    ports:
      - 8081:8081
    network_mode: host

  httpbin:
    image: kennethreitz/httpbin
    ports:
      - 80:80
